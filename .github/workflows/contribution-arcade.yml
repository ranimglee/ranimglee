name: Generate contribution arcade

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Generate snake contribution graph
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ranimglee
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: Generate Pac-Man contribution graph
        uses: abozanona/pacman-contribution-graph@main
        with:
          github_user_name: ranimglee

      - name: Move Pac-Man assets into dist
        shell: bash
        run: |
          mkdir -p dist
          if [ -f pacman-contribution-graph.svg ]; then mv pacman-contribution-graph.svg dist/pacman-contribution-graph.svg; fi
          if [ -f pacman-contribution-graph-dark.svg ]; then mv pacman-contribution-graph-dark.svg dist/pacman-contribution-graph-dark.svg; fi

      - name: Publish arcade assets
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
